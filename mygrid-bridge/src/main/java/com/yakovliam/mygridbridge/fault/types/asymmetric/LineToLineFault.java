package com.yakovliam.mygridbridge.fault.types.asymmetric;

/**
 * Represents a line-to-line fault.
 * <p>
 * A line-to-line fault is a fault where the fault current flows between two lines.
 * Commonly caused by a failed insulator or objects falling on the lines.
 *
 * @author Jacob C.
 */
public abstract class LineToLineFault extends AsymmetricFault {
}
